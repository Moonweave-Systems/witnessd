__pycache__/
*.pyc
.venv/
# Ignore private key material under any keys/ dir, but keep public keys tracked
# so fixture revalidation (which needs operator.pub) passes in CI.
**/keys/*
!**/keys/*.pub

# Transient W10 live-capture sandbox (run_w10_live.py); never commit
fixtures/w10-sandbox/

# Python build/editable-install artifacts
*.egg-info/
build/
dist/
__pycache__/
