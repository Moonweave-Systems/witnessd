- Plan said the new claimless `check_commands` validation was sufficient for `validate_role_lane_plan`; the code also had a later generic non-empty `region` requirement, so I exempted only lanes explicitly declaring `lane_intent="verification-only"` while preserving non-empty regions for every other lane.
- Plan said Task 6's verification-only end-to-end test would pass after the fan-in change; the proofrun path also translated every role lane into a write-oriented planner packet whose validator rejects an empty region, so I excluded only claimless verification-only lanes from that sealed write-plan packet list while still executing them from the bound role-lane plan.
- Plan identified one public-flow forbidden-profile loop to update; the code had a second phase-forbidden loop that also included `verification-only`, so I removed it from both loops and left `critic-only` and `review-only` blocked.
