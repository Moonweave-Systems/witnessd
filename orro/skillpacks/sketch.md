---
name: orro-sketch-method
mode: sketch
triggers: sketch, ideation, brainstorm, design direction, candidate approaches
boundary: advisory-only
---

# Migration note

Use `orro advise "<goal>" --mode sketch` for an explicit sketch route. The
default `orro advise` router selects this path for new-work goals and preserves
the existing sketch artifact and provenance contract. `orro sketch` remains a
deprecated alias for one release.
