#!/bin/sh
if [ "$1" = "--version" ]; then echo "codex-cli 0.0.0"; exit 0; fi
out=""
while [ $# -gt 0 ]; do
  if [ "$1" = "--output-last-message" ]; then out="$2"; fi
  shift
done
: > "$out"
echo done >> "$out"
exit 0
