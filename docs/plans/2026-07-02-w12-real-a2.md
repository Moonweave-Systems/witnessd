# W12 — Real A2: uid 격리를 실제로 세우고 demonstration 딱지를 증거로 제거 (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.
> 스펙 근거: `SPEC.md` §3.4(uid 모델)·Part I Decision(uid 1급) + `SPEC2.md` §4 (D12-1~D12-3).
> **주의: 이 웨이브는 sudo가 필요한 호스트 작업 포함 — 해당 스텝은 운영자 승인/실행.**

**Goal:** A2(`A2-isolated-observed`)는 제품 assurance 사다리의 최상단인데 지금까지 이 호스트에
uid 격리가 없어 **demonstration-only**였다(`fixtures/w1/A2-DEMONSTRATION.md`). 이 웨이브는 전용
observer 계정을 실제로 만들어(호스트 sudo 가능 실측 확인됨, 2026-07-02) 진짜 격리 run이
A2를 방출하고 Depone strict assert가 통과하게 한 뒤, **그 증거로만** demonstration 딱지를 제거한다.

**불변식:** 문서 먼저 제거 금지(D12-2 — 증거가 먼저) / `isolation.py` 기존 fail-closed 로직 재사용
(재작성 금지) / observer_dir 0700·runner 쓰기 불가 / root(uid 0) 실행은 기존대로 fail-closed 유지.

## Task 0: 현재 격리 판정 로직 실측
- [ ] `witnessd/isolation.py`(`verify_isolation_boundary`, `probe_lane_isolation`)와 Depone
  `agent_fabric/isolation.py`의 A2 판정 조건을 실코드로 확정(추측 금지): 요구 facts 필드,
  `runner_uid != observer_uid != 0`, observer_dir 권한 검사.
- [ ] 현재 데모 fixture(`fixtures/w1/capture-manifest-a2.json`)가 무엇을 흉내내는지 기록.

## Task 1: 호스트 셋업 (운영자 스텝 — 에이전트는 스크립트만 준비)
- [ ] `scripts/setup_a2_host.sh` 작성(멱등): `witnessd-observer` 시스템 계정 생성,
  observer_dir 소유·0700, runner(ubuntu uid 1001)가 쓰기 불가 확인, sudoers 최소 항목
  (observer로 관측 프로세스 기동에 필요한 최소 권한만) 문서화. **스크립트는 에이전트가 작성,
  실행은 운영자가 검토 후**(`sudo bash scripts/setup_a2_host.sh`).
- [ ] 검증 스텝 포함: 셋업 후 `uv run python3 -m witnessd isolation --self-test`가 실격리 조건에서
  통과.

## Task 2: 격리 run 배선 (RED→GREEN)
- [ ] RED: observer 프로세스를 `witnessd-observer` uid로 기동하는 경로(`sudo -u witnessd-observer`
  또는 setuid 헬퍼 — 기존 supervisor/observer 코드에 최소 침습) + probe가 실제 facts
  (`runner_uid=1001, observer_uid=<신규>, observer_dir_mode=0700`)를 캡처.
- [ ] GREEN: 셸 레인 1회를 실격리로 실행 → capture-manifest `assurance=="A2-isolated-observed"`.
- [ ] 커밋.

## Task 3: 증거 봉인 + demonstration 딱지 제거
- [ ] 실격리 run의 evidence를 `fixtures/w12/`에 커밋(공개키만).
- [ ] `scripts/revalidate_w12.py`: Depone `validate_capture_manifest` + isolation strict assert가
  **demonstration 분기 없이** A2를 재도출. negative: runner-writable observer_dir facts → blocked.
- [ ] 이제서야: `fixtures/w1/A2-DEMONSTRATION.md` 제거 + `revalidate_w1.py`의 demonstration 분기
  를 strict로 전환 + README/conformance의 "A2 demonstration-only" 문구를 "실격리 실증(호스트 요건:
  전용 observer uid)"로 갱신.
- [ ] 회귀: w1~w11 전부 그린.

## Task 4: CI (선택, D12-3)
- [ ] GitHub 러너는 sudo 가능 — CI job `real-a2`로 계정 생성+격리 run 재현 시도. 성공 시 required,
  불안정하면 non-blocking + 로컬 fixture 재도출을 required로(정직 표기).

## Final Validation Matrix
```bash
PYTHONPATH=/home/ubuntu/moonweave/depone uv run python3 -m unittest discover -s tests
for s in w1 w2 w3 w4 w5 w7 w8 w10 w11 w12 key_rotation; do PYTHONPATH=/home/ubuntu/moonweave/depone uv run python3 scripts/revalidate_$s.py; done
grep -rn "A2-DEMONSTRATION\|demonstration" README.md docs/conformance/ && exit 1 || echo "딱지 제거 확인"
```

**Explicit Non-Changes:** docker/container 격리 모델 1급 승격 금지(Part I Decision 유지) /
root 실행 fail-closed 완화 금지 / isolation 판정 조건 완화 금지 / 운영자 승인 없는 sudo 실행 금지.
